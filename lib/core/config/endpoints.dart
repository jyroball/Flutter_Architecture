class Endpoints {
  static const baseUrl = "https://jsonplaceholder.typicode.com"; // Example

  // Posts endpoint
  static const posts = "/posts";
  static const users = "/users";

  // If you have other endpoints, list them here...
}